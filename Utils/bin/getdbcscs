#!/bin/bash

TOPDIR=/dbc/pa-dbc1104/kulkarnir/vrack/src/calaveras-dev/glcm
SCSDIR=$TOPDIR/platform/services/scs
cd $HOME/src
[ -d dbc ] || mkdir dbc
cd dbc
scp -r pa-dbc1104.eng.vmware.com:$SCSDIR scs
